r"""
Fields
======

A **field** is a physical quantity, represented by a scalar, vector, spinor, or tensor, that has a
value for each point in space and time.

.. _general-flux-def:

One common vector field operation is the **flux** of a vector field :math:`\vec F` through a 
surface :math:`A`, defined as the surface integral

.. math::
    \Phi_F = \iint_A \vec F \cdot d \vec A,

where :math:`d \vec A` is the infinitesimal vector area of surface :math:`A` co-directed with the 
surface normal.

**Links:**

#. `Wikipedia — Flux — Flux as a surface integral <https://en.wikipedia.org/wiki/Flux#Flux_as_a_surface_integral>`__
"""
